# Новые функции

## 1. Темный/Светлый режим

### Описание
Реализована возможность переключения между темным и светлым режимами интерфейса.

### Реализация
- **ThemeContext** (`frontend/src/context/ThemeContext.js`) - контекст для управления темой
- **Переключатель темы** - кнопка в шапке Dashboard (🌙/☀️)
- **CSS переменные** - обновлены в `index.css` для поддержки темной темы
- **Сохранение предпочтений** - тема сохраняется в localStorage

### Использование
1. Нажмите на кнопку 🌙/☀️ в правом верхнем углу Dashboard
2. Тема переключается между светлой и темной
3. Предпочтение сохраняется автоматически

### Файлы
- `frontend/src/context/ThemeContext.js`
- `frontend/src/components/Dashboard.js` (обновлен)
- `frontend/src/App.js` (обновлен)
- `frontend/src/index.css` (обновлен)
- `frontend/src/components/Dashboard.css` (обновлен)

---

## 2. Продажа автомобиля

### Описание
Пользователи могут создавать объявления о продаже своих автомобилей.

### Реализация

#### Backend
- **Модель Car** - добавлено поле `sellerId` для отслеживания продавца
- **Endpoint** `POST /api/cars/sell` - создание объявления о продаже
- **Валидация** - проверка обязательных полей, года, цены, пробега
- **Загрузка фото** - поддержка до 10 фотографий (до 5MB каждая)
- **Статус** - новые объявления получают статус `pending` (требуют модерации)

#### Frontend
- **Компонент SellCar** - форма для создания объявления
- **Вкладка "Продать"** - добавлена в Dashboard
- **Валидация формы** - проверка данных на клиенте
- **Загрузка файлов** - поддержка множественной загрузки фотографий

### Использование
1. Перейдите на вкладку "🚙 Продать" в Dashboard
2. Заполните форму с информацией об автомобиле
3. Загрузите фотографии (опционально, до 10 штук)
4. Нажмите "Разместить объявление"
5. Объявление будет отправлено на модерацию администратору

### Обязательные поля
- Марка
- Модель
- Год выпуска
- Цена

### Опциональные поля
- Пробег
- Цвет
- Двигатель
- Коробка передач
- Тип топлива
- Город
- Описание
- Фотографии

### Файлы
- `backend/models/Car.js` (обновлен)
- `backend/routes/cars.js` (обновлен)
- `backend/server.js` (обновлен - добавлена связь User-Car)
- `frontend/src/components/SellCar.js` (новый)
- `frontend/src/components/SellCar.css` (новый)
- `frontend/src/components/Dashboard.js` (обновлен)

---

## 3. Тесты

### Backend тесты
- **Файл**: `backend/tests/cars.sell.test.js`
- **Покрытие**: 
  - Создание объявления с валидными данными
  - Валидация обязательных полей
  - Валидация года, цены, пробега
  - Сохранение sellerId
  - Установка статуса pending

### Frontend тесты
- **ThemeContext.test.js** - тесты для темного/светлого режима
- **SellCar.test.js** - тесты для компонента продажи автомобиля

### Запуск тестов

#### Backend
```bash
cd backend
npm install  # Установите зависимости (jest, supertest)
npm test
```

#### Frontend
```bash
cd frontend
npm install  # Установите зависимости (@testing-library/react и др.)
npm test
```

### Файлы
- `backend/tests/cars.sell.test.js`
- `backend/tests/README.md`
- `frontend/src/components/__tests__/ThemeContext.test.js`
- `frontend/src/components/__tests__/SellCar.test.js`
- `frontend/src/components/__tests__/README.md`
- `backend/package.json` (обновлен - добавлены тестовые зависимости)
- `frontend/package.json` (обновлен - добавлены тестовые зависимости)

---

## Установка зависимостей

### Backend
```bash
cd backend
npm install
```

Новые зависимости для тестов:
- `jest` - фреймворк для тестирования
- `supertest` - HTTP assertions для тестирования API

### Frontend
```bash
cd frontend
npm install
```

Новые зависимости для тестов:
- `@testing-library/react` - утилиты для тестирования React компонентов
- `@testing-library/jest-dom` - дополнительные matchers для Jest
- `@testing-library/user-event` - симуляция пользовательских событий

---

## Миграция базы данных

После обновления модели Car необходимо синхронизировать базу данных:

```bash
cd backend
npm start
```

Сервер автоматически добавит новое поле `sellerId` в таблицу `cars` при запуске (благодаря `sequelize.sync({ alter: true })`).

---

## Примечания

1. **Модерация объявлений**: Новые объявления получают статус `pending` и требуют одобрения администратором через админ-панель.

2. **Темный режим**: Тема сохраняется в localStorage и автоматически применяется при следующем посещении.

3. **Загрузка файлов**: Фотографии сохраняются в `backend/uploads/cars/` и доступны по URL `/uploads/cars/filename`.

4. **Валидация**: Валидация выполняется как на клиенте, так и на сервере для обеспечения безопасности.
