# Реализованные функции

## ✅ 1. Система рейтинга продавца (Seller Rating)

### Backend:
- **Модель**: `backend/models/SellerRating.js`
  - `seller_id` - ID продавца
  - `user_id` - ID пользователя, который оценивает
  - `rating` - Рейтинг от 1 до 5 звезд
  - `comment` - Комментарий (опционально)

- **API Routes**: `backend/routes/sellerRatings.js`
  - `POST /api/seller-ratings` - Создать/обновить рейтинг
  - `GET /api/seller-ratings/seller/:sellerId` - Получить все рейтинги продавца
  - `GET /api/seller-ratings/seller/:sellerId/my` - Получить мой рейтинг для продавца

### Frontend:
- **Компонент**: `frontend/src/components/SellerRating.js`
  - Интерактивный звездный рейтинг (1-5 звезд)
  - Отображение среднего рейтинга
  - Список всех оценок с комментариями
  - Интегрирован в `CarDetail.js`

### Использование:
Рейтинг продавца отображается на странице деталей автомобиля для всех пользователей, кроме владельца автомобиля.

---

## ✅ 2. Счетчик просмотров (Views Counter)

### Backend:
- **Модель**: `backend/models/Car.js`
  - Поле `views` (INTEGER, default: 0)
  - Автоматически увеличивается при каждом просмотре страницы автомобиля

- **API**: `backend/routes/cars.js`
  - При запросе `GET /api/cars/:id` счетчик увеличивается на 1
  - Утилита `backend/utils/addViewsColumn.js` добавляет столбец при первом запуске

### Frontend:
- **Отображение**:
  - В списке автомобилей (`CarsList.js`): "👁️ Просмотров: 1523"
  - На странице деталей (`CarDetail.js`): "👁️ Просмотров: 1523"

### Аналитика:
Счетчик просмотров позволяет:
- Отслеживать популярность автомобилей
- Показывать наиболее просматриваемые объявления
- Анализировать интерес пользователей

---

## Проверка работы

### Для проверки рейтинга продавца:
1. Откройте страницу любого автомобиля (не своего)
2. Прокрутите вниз до секции "⭐ Рейтинг продавца"
3. Выберите рейтинг (1-5 звезд)
4. Добавьте комментарий (опционально)
5. Нажмите "Отправить рейтинг"
6. Проверьте, что средний рейтинг обновился

### Для проверки счетчика просмотров:
1. Откройте страницу любого автомобиля
2. Проверьте, что отображается "👁️ Просмотров: X"
3. Обновите страницу - счетчик должен увеличиться
4. В списке автомобилей также должен отображаться счетчик просмотров

---

## Технические детали

### База данных:
- Таблица `seller_ratings` создается автоматически при запуске
- Столбец `views` в таблице `cars` добавляется автоматически при первом запуске
- Все связи настроены в `server.js`

### Безопасность:
- Рейтинг можно оставить только авторизованным пользователям
- Нельзя оценить самого себя
- Один пользователь может оставить только один рейтинг (обновляется при повторной оценке)
